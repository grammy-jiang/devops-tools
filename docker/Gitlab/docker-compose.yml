# Create Docker Network

# docker network create \
#     --driver macvlan \
#     --subnet 10.0.0.0/8 \
#     --gateway 10.0.0.1  \
#     --opt parent enp0s31f6 \
#     pub_net

# https://docs.gitlab.com/omnibus/docker/README.html
# Use the following command to edit gitlab.rb:
# sudo docker exec -it dc-gitlab vi /etc/gitlab/gitlab.rb
# sudo docker exec -it dc-gitlab gitlab-ctl reconfigure

version: '3'
services:
    gitlab:
        container_name: dc-gitlab
        image: gitlab/gitlab-ce:latest@sha256:7b4c96abe9f068615fcab1f5a658fb1d34a01b6905398c447287aac1a5663c65
        environment:
            GITLAB_ROOT_PASSWORD: xxxx
            GITLAB_OMNIBUS_CONFIG: |
                external_url 'http://10.0.20.0'
        expose:
            - '22'
            - '80'
            - '433'
        hostname: 10.0.20.0
        networks:
            pub_net:
                ipv4_address: 10.0.20.0
        restart: always
        tty: true
        volumes:
            - ./gitlab-data:/var/opt/gitlab
            - ./gitlab-log:/var/log/gitlab
            - ./gitlab-config:/etc/gitlab
networks:
    pub_net:
        external: true
